<%= form_with(model: categorium, local: true) do |form| %>
  <% if categorium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categorium.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% categorium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
    <%= form.label :idCategory %>
    <%= form.number_field :idCategory, id: :category_id %>
  </div>

  <div class="field">
    <%= form.label :label %>
    <%= form.text_field :label, id: :category_label%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>